package com.devendrabrain.orderby.version.orderby.version.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.devendrabrain.orderby.version.orderby.version.entity.Address;

public interface AddressRepository extends JpaRepository<Address, Long>{

}
